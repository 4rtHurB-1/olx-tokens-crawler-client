extends layout

block content
  a(href=`/results`) Результати
  h1= title
  p #{info}
  form(method='post', enctype='multipart/form-data')
    input(type='file', name='imageupload')
    input(type='submit', name='uploadimage', value='Завантажити')
